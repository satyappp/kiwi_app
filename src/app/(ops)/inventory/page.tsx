import { InventoryScreen } from "@/features/inventory";

/** Thin route: the inventory feature owns data access and presentation. */
export default function InventoryPage() {
  return <InventoryScreen />;
}
