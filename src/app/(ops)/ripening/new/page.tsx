import { NewRipeningScreen } from "@/features/ripening";

/** Thin route: the ripening feature owns data access and presentation. */
export default function NewRipeningPage() {
  return <NewRipeningScreen />;
}

